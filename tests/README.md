# Shared Validation Roots

- `e2e/`: integration-owner end-to-end journeys.
- `fixtures/shared/`: deterministic producer and tenant fixtures.
- `regression/`: fixed retrieval, question and Evidence cases.
- `load/`: pilot and staged concurrency scenarios.

Lane-specific unit, contract and integration tests remain under `backend/tests/` or their owning SPA.
