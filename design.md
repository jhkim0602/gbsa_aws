# Interview Evidence Platform Design

## 1. 문서 목적

이 문서는 현재 코드에 구현된 Interview Evidence Platform의 UI/UX 구조와
디자인 규칙을 정리한다. 기업담당자용 운영 화면과 지원자용 면접 화면은 서로
다른 사용 맥락을 가지지만 동일한 제품 언어와 상태 체계를 사용한다.

- 기준일: 2026-08-19
- 기업담당자 SPA: `apps/company-console`
- 지원자 SPA: `apps/applicant-interview`
- 공통 API: FastAPI 기반 `backend`

## 2. 제품 구조

| 제품        | 주요 사용자         | 목적                                     | 로컬 주소                       |
| ----------- | ------------------- | ---------------------------------------- | ------------------------------- |
| 기업 콘솔   | 채용 담당자, 검토자 | 포지션 운영, 지원자 관리, 면접 결과 검토 | `http://127.0.0.1:5183/company` |
| 지원자 포털 | 면접 지원자         | 초대 확인, 자료 제출, 환경 점검, AI 면접 | `http://127.0.0.1:5184/access`  |

두 SPA는 React Router 기반으로 독립 실행된다. 화면 컴포넌트는 도메인별
`features/` 디렉터리에 위치하고 API 변환은 각 앱의
`src/app/routeAdapters.tsx`에서 담당한다.

## 3. 핵심 디자인 원칙

### 업무 중심

- 기업 콘솔은 반복 업무와 비교가 중심인 조용한 운영 도구로 설계한다.
- 큰 마케팅형 히어로 대신 탐색, 표, 상태, 작업 버튼을 우선한다.
- 카드 중첩을 피하고 페이지 섹션과 반복 항목의 위계를 명확히 구분한다.

### 단계 중심

- 지원자 포털은 현재 위치와 다음 행동을 항상 알 수 있어야 한다.
- 상단 여정 표시와 화면 내 단계 번호를 함께 사용한다.
- 한 화면의 주요 행동은 하나로 제한하고 부가 행동은 보조 버튼으로 표현한다.

### 상태 중심

- `대기`, `진행 중`, `완료`, `부분 완료`, `실패`를 일관된 색과 문구로 표시한다.
- 색상만으로 상태를 전달하지 않고 텍스트와 아이콘 또는 형태를 함께 사용한다.
- 비동기 작업은 비활성 버튼, 상태 문구, 진행 표시를 동시에 제공한다.

### 근거와 신뢰

- AI 분석 결과와 사람의 최종 판단을 명확히 구분한다.
- 기술 장애나 분석 실패가 지원자 역량으로 오인되지 않도록 안내한다.
- 서버가 반환한 완료 상태와 화면용 진행 애니메이션을 혼동하지 않는다.

## 4. 정보 구조

### 기업 콘솔

| 경로                                              | 화면                   |
| ------------------------------------------------- | ---------------------- |
| `/company`                                        | 채용 운영 대시보드     |
| `/positions`                                      | 채용 포지션 목록       |
| `/positions/:positionId`                          | 포지션 운영 상세       |
| `/applicants`                                     | 전체 지원자 관리       |
| `/positions/:positionId/applicants/:invitationId` | 지원자 상세            |
| `/hiring/*`                                       | 채용 생성 및 설정 흐름 |
| `/settings/invitation-email`                      | 초대 메일 템플릿       |
| `/review/:sessionId`                              | 면접 근거 및 결과 검토 |

기업 콘솔은 고정 사이드바, 상단 페이지 컨텍스트, 본문 작업 영역으로 구성한다.
모바일에서는 사이드바를 오버레이 탐색으로 전환한다.

#### 채용 생성 4단계

```text
1. 포지션 정보
   ├─ 포지션명
   ├─ 직무 카테고리
   ├─ 세부 직무
   │  ├─ 직접 입력
   │  └─ 추천 직무 선택
   └─ 면접 운영 설정
      ├─ 면접 인원
      └─ 면접 시각

2. 지원자 제출
   ├─ 역할 설명
   └─ 필수 제출 자료
      ├─ 이력서
      ├─ 자기소개서
      ├─ 경력기술서
      ├─ 대표 프로젝트
      └─ 포트폴리오

3. 평가 설계
   ├─ 직무 요구사항
   │  ├─ 필수·우대 구분
   │  ├─ 요구사항
   │  ├─ 중요도
   │  └─ 연결 평가기준
   └─ 평가기준
      ├─ 이름·설명·가중치
      ├─ 필수 여부
      └─ 질문·검증 가이드

4. 면접 운영
   ├─ 면접 시간
   ├─ 면접 난이도
   ├─ 금지 주제
   └─ 포지션·평가기준 게시
```

세부 직무를 선택하거나 직접 입력하면 해당 명칭을 포지션명 입력에 제안값으로
복사한다. 포지션명은 복사된 이후에도 채용 담당자가 자유롭게 수정할 수 있다.
네 단계의 데이터는 마지막 게시 시 기존 포지션 생성 API와 평가기준 게시 API에
순서대로 전달한다.

### 지원자 포털

| 단계 | 경로                  | 화면                       |
| ---- | --------------------- | -------------------------- |
| 1    | `/access/*`           | 초대 확인, 본인 확인, 동의 |
| 2    | `/submissions/*`      | 문서 및 공개 프로젝트 제출 |
| 3    | `/interview/*`        | 장비 및 네트워크 점검      |
| 4    | `/interview/session`  | AI 면접 진행               |
| 5    | `/interview/complete` | 면접 완료                  |

지원자 포털은 상단 제품 바와 5단계 여정을 유지한다. 본문은 화면 성격에 따라
최대 너비를 제한해 집중도를 높인다.

## 5. 시각 체계

두 SPA의 스타일은 Tailwind CSS v4 단일 파이프라인으로 빌드한다. 공통 색상,
폰트, 반경, 그림자는 `@iep/design-system/theme.css`의 `@theme`에서 관리하고,
화면별 의미가 필요한 복합 스타일은 Tailwind `components` 레이어에 둔다.
기능 컴포넌트에서 개별 CSS 파일을 직접 로드하지 않는다.

### 색상

| 역할           | 값        |
| -------------- | --------- |
| 배경           | `#f5f6f8` |
| 기본 표면      | `#ffffff` |
| 강조 표면      | `#eef0f4` |
| 기본 텍스트    | `#1a1f36` |
| 보조 텍스트    | `#75788b` |
| 주요 액션      | `#5966ce` |
| 주요 액션 활성 | `#4653bd` |
| 성공           | `#1e9e63` |
| 경고           | `#c77d11` |
| 오류           | `#d64545` |

주요 색은 액션과 현재 단계에 사용하고, 화면 전체를 단일 색조로 채우지 않는다.
성공·경고·오류 색은 상태 표현에만 제한적으로 사용한다.

### 타이포그래피

- 기본: `Inter`, `Pretendard`, 시스템 산세리프
- 화면 제목: 23-28px
- 섹션 제목: 15-20px
- 본문: 13-14px
- 보조 정보: 10-12px
- 영문 상태 라벨과 단계 표시는 모노스페이스 계열을 제한적으로 사용한다.
- 자간은 `0`을 유지한다.

### 형태

- 일반 패널과 입력: 8-10px 모서리
- 상태 배지: 원형 pill
- 아이콘 버튼: 정사각형 32-44px
- 과도한 그림자 대신 얇은 경계와 낮은 명도의 그림자를 사용한다.
- 개별 반복 항목만 카드로 표현하고 페이지 섹션 자체는 카드처럼 띄우지 않는다.

## 6. 주요 컴포넌트 규칙

### 버튼

- 주요 버튼: 채워진 인디고 배경
- 보조 버튼: 흰색 배경과 회색 경계
- 위험 작업: 빨간 텍스트 또는 위험 배경
- 아이콘으로 충분한 명령은 Lucide 아이콘과 툴팁 또는 접근성 라벨을 사용한다.
- 비활성 상태는 명도와 커서로 구분하며 레이아웃 크기는 유지한다.

### 입력

- 컨트롤 높이는 기본 44px이다.
- 라벨은 입력 위에 배치하고 placeholder는 예시 용도로만 사용한다.
- 파일 입력은 파일 종류, 선택 파일명, 크기, 실행 버튼을 한 영역에 표시한다.
- 오류는 입력 근처에 텍스트로 제공한다.

### 상태

- 상태 배지는 짧은 명사형 문구를 사용한다.
- 분석 상태는 정적 문구로 표시하고 반복 애니메이션을 사용하지 않는다.
- 실제 완료·실패 판정은 API 응답을 기준으로 한다.

## 7. 자료 제출 화면

현재 자료 제출 화면은 하나의 2열 작업 패널로 구성한다.

- 왼쪽 열: 이력서, 자기소개서, 경력기술서, 대표 프로젝트, 포트폴리오 목록
- 목록 상태: 자료별 제출 상태와 분석 상태를 함께 표시
- 오른쪽 열: 선택 자료의 파일 또는 URL 입력과 제출 액션
- 분석 상태: 제출 성공 후 기존 상태 API 결과를 목록에 자동 반영

자료 목록은 개별 카드가 아니라 표 형태의 행으로 표시한다. 분석 영역에는
별도 확인 버튼, 스피너, 자동 진행률, 단계 애니메이션을 사용하지 않는다.

## 8. 반응형 규칙

- `860px` 이하: 지원자 여정 텍스트 탐색을 축약한다.
- `680px` 이하: 기업 화면의 페이지 여백과 헤더를 축소한다.
- `600px` 이하: 지원자 카드와 버튼을 단일 열 중심으로 재배치한다.
- `430px` 이하: 긴 보조 액션을 전체 너비로 전환한다.
- 고정 형식 요소에는 명시적인 너비, 높이, grid track을 사용해 상태 변화 시
  레이아웃 이동을 방지한다.

## 9. 접근성

- 모든 화면은 의미 있는 `main`, `nav`, `header`, `section` 구조를 사용한다.
- 폼 입력은 연결된 `label` 또는 접근성 이름을 가진다.
- 현재 단계는 `aria-current="step"`으로 표시한다.
- 비동기 결과는 `role="status"` 또는 `role="alert"`로 알린다.
- 키보드 포커스에는 2px 인디고 외곽선을 제공한다.
- 모션 감소 설정을 존중한다.

## 10. 구현 위치

| 항목                 | 파일                                                    |
| -------------------- | ------------------------------------------------------- |
| 공통 Tailwind 테마   | `packages/design-system/theme.css`                      |
| 기업 스타일 진입점   | `apps/company-console/src/app/styles/index.css`         |
| 기업 라우트          | `apps/company-console/src/app/featureRoutes.ts`         |
| 기업 셸              | `apps/company-console/src/app/layouts/CompanyShell.tsx` |
| 지원자 스타일 진입점 | `apps/applicant-interview/src/app/styles/index.css`     |
| 지원자 라우트        | `apps/applicant-interview/src/app/featureRoutes.ts`     |
| 지원자 셸            | `apps/applicant-interview/src/app/ApplicantShell.tsx`   |
| 자료 제출            | `apps/applicant-interview/src/features/submissions/`    |
| 면접 화면            | `apps/applicant-interview/src/features/interview/`      |

## 11. 개발 원칙

- 기능 작업은 `develop`에서 분기한 `feature/<기능명>` 브랜치에서 진행한다.
- 기능 브랜치를 `develop`에 병합한 뒤 `develop`에서 `main`으로 PR을 생성한다.
- `main` 병합 PR을 기준으로 CI/CD를 실행한다.
- UI 변경은 관련 기능 디렉터리에 한정하고 API 계약 변경과 분리한다.
- 대규모 스타일 변경은 두 SPA 프로덕션 빌드와 대표 화면 브라우저 검증을 수행한다.
