{
  "name": "interview-evidence-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Interview Evidence Platform.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev:applicant": "npm run dev --workspace @iep/applicant-interview",
    "dev:company": "npm run dev --workspace @iep/company-console",
    "format": "prettier --write . && UV_CACHE_DIR=.uv-cache uv run --no-sync ruff format .",
    "format:check": "prettier --check . && UV_CACHE_DIR=.uv-cache uv run --no-sync ruff format --check .",
    "lint": "eslint . && UV_CACHE_DIR=.uv-cache uv run --no-sync ruff check .",
    "typecheck": "npm run typecheck --workspaces --if-present && UV_CACHE_DIR=.uv-cache uv run --no-sync mypy backend/src"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "globals": "^15.14.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.0"
  }
}
